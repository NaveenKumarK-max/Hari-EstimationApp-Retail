/**
 * Keyboard options class
 */
export declare class KeyboardOptions {
    scrollPadding: boolean;
    scrollAssist: boolean;
    constructor(options?: KeyboardOptionsModel);
}
/**
 * Keyboard options model
 */
export interface KeyboardOptionsModel {
    scrollPadding: boolean;
    scrollAssist: boolean;
}
